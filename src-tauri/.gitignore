# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# ffmpeg/ffprobe sidecars are fetched per platform, not committed.
# Run `scripts/fetch-sidecars.sh` to populate them (README.md stays tracked).
/binaries/ffmpeg-*
/binaries/ffprobe-*

# libmpv-2.dll is fetched for the Windows bundle, not committed (ADR 0014).
# Run `scripts/fetch-libmpv.sh` to populate it.
/libmpv-2.dll
